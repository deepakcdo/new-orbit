package org.commerzbank.instrument.streamer;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeInstrumentStreamerIT extends InstrumentStreamerTest {

    // Execute the same tests but in native mode.
}